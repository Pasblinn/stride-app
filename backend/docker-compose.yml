version: "3.3"

services:
  database: 
    image: postgres
    container_name: stride-app
    restart: always
    ports: 
      - 5432:5432
    environment:
      - POSTGRES_USER=stride_user
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=stride_db
      - TZ=America/Sao_Paulo
      - PGTZ=America/Sao_Paulo
    volumes:
      - pgdata:/data/postgres

volumes:
  pgdata:
    driver: local

networks:
  default:
    driver: bridge